# AGENTS.md

## 적용 범위

- 이 문서는 저장소 루트와 모든 하위 디렉터리에 적용합니다.
- 하위 디렉터리에 별도의 `AGENTS.md`가 있으면 해당 범위에서는 더 가까운 지침을 우선합니다.
- 작업 전 `README.md`, `STRUCTURE.md`와 관련 `docs/`, `configs/`, `experiments/manifests/`를 먼저 확인합니다.

## 대화와 작업 원칙

- 사용자에게 보내는 모든 메시지는 한국어 존댓말로 작성합니다.
- 요청 범위 안에서 필요한 변경과 검증까지 완료합니다.
- 확인한 사실, 추정, 계획과 실제 검증 결과를 명확히 구분합니다.
- 구현되지 않았거나 실행하지 않은 기능과 실험 결과를 완료된 것처럼 표현하지 않습니다.
- 기존 사용자 변경사항과 요청 범위 밖의 파일은 수정하거나 되돌리지 않습니다.
- 모든 내용은 주어진 자료, 논문의 내용을 검토하고 답하며, 모르는 것은 모른다고 답합니다.

## 디렉터리 경계

- `src/vlade/`: VLADE 핵심 구현을 둡니다.
- `scripts/`: 학습, 평가, 데이터 처리와 배포의 얇은 실행 진입점만 둡니다.
- `benchmarks/`: LIBERO, LIBERO+, CALVIN별 연결 코드와 환경 차이를 흡수합니다.
- `configs/`: 데이터, 모델, 증류, 실험과 배포 설정을 관리합니다.
- `experiments/`: baseline, ablation, 실행 manifest와 재현 가능한 실험 정의를 관리합니다.
- `tests/`: 단위, 통합과 smoke test를 구분해 둡니다.
- `docs/`: 설계 결정, 환경 구성, 평가 프로토콜과 논문 관련 기록을 둡니다.
- `notebooks/`: 탐색과 분석에만 사용하며 핵심 로직은 `src/vlade/`로 옮깁니다.
- `data/`, `checkpoints/`, `artifacts/`: 로컬 데이터, 가중치와 생성 결과를 보관하며 대용량 파일은 Git에 추가하지 않습니다.
- `.harness/`: 에이전트가 작업하면서 만든 조사 결과, 작업 메모, 임시 보고서와 중간 산출물을 작업별 하위 폴더에 보관합니다. 정식 소스, 설정, 문서와 공식 실험 결과를 대신하는 위치로 사용하지 않습니다.
- `.obsidian/`: 개인 문서 작업 공간 설정이므로 명시적인 요청이 없으면 수정하지 않습니다.

디렉터리 구조를 추가하거나 역할을 바꾸면 `README.md`와 `STRUCTURE.md`도 함께 갱신합니다.

## `.harness/` 작업 규칙

- 현재 형식 버전은 `v0/`이며 작업별 경로는 원칙적으로 `.harness/v0/YYYY-MM-DD-작업명/` 형식을 사용합니다.
- 날짜는 한국 표준시 기준으로 기록하고 작업명은 짧고 구체적인 kebab-case 영문을 권장합니다.
- 작업 폴더에는 필요한 파일만 만들며 빈 하위 폴더를 미리 생성하지 않습니다.
- 파일이 여러 개이거나 맥락 설명이 필요하면 작업 폴더의 `README.md`에 목적, 입력, 수행 내용, 결과, 검증과 남은 사항을 요약합니다.
- 필요에 따라 `notes/`, `reports/`, `logs/`, `outputs/`를 사용하되 정식 프로젝트 파일을 대신하는 위치로 사용하지 않습니다.
- 파일명은 내용이 드러나게 작성하고 `final`, `new`, `latest`, `v2`처럼 의미가 불분명한 이름은 피합니다.
- 조사 기록에는 가능한 범위에서 출처 URL, 저장소 내 원본 경로 또는 관련 commit을 남깁니다.
- 명령 결과에는 실행 목적과 환경을 함께 기록하고 로그를 임의로 수정하거나 성공한 것처럼 꾸미지 않습니다.
- 저장소 파일 전체를 복제하지 말고 원본 경로를 참조하며 필요한 발췌만 최소한으로 보관합니다.
- 토큰, 비밀번호, API 키, `.env` 내용, 개인정보와 기타 비밀정보를 저장하지 않습니다.
- 데이터셋, 모델 가중치, 대용량 로그, 영상과 빌드 결과는 복사하지 않고 지정된 프로젝트 경로와 설명만 기록합니다.
- 외부 문서나 논문의 전문을 복제하지 않고 링크, 서지정보와 필요한 요약만 기록합니다.
- 기존 작업 기록은 명시적인 요청 없이 삭제하거나 덮어쓰지 않습니다.
- 작업 완료 전 일회성 scratch 파일, 중복본과 의미 없는 로그를 정리합니다.
- Git에는 재현이나 의사결정에 도움이 되는 작고 안전한 기록만 추가합니다.
- `.harness/`에 산출물을 남겼다면 완료 보고에 해당 경로와 용도를 명시합니다.

## 외부 소스 규칙

- `third_party/ava_vla/`는 고정된 외부 Git submodule이며 기본적으로 읽기 전용으로 취급합니다.
- 외부 코드를 직접 수정하지 않고 VLADE 측 어댑터와 확장 로직을 `src/vlade/`에 구현합니다.
- 명시적인 요청 없이 submodule URL, 브랜치 또는 고정 커밋을 변경하지 않습니다.
- 외부 코드를 인용하거나 가져올 때 원본 출처, 라이선스와 변경 내역을 보존합니다.

## 구현 원칙

- Teacher와 Student의 차이는 공통 인터페이스 뒤에 격리하여 학습 코드가 특정 모델 구현에 직접 의존하지 않도록 합니다.
- benchmark별 observation/action 변환은 `benchmarks/` 또는 데이터 어댑터 경계에서 처리합니다.
- 로컬 절대 경로, 토큰, 비밀번호와 개인 환경값을 코드나 설정에 기록하지 않습니다.
- 디바이스, dtype, seed, 경로와 주요 하이퍼파라미터는 설정으로 제어할 수 있게 합니다.
- 새 Python 코드는 책임이 분명한 작은 단위로 작성하고 공개 인터페이스에는 타입과 필요한 설명을 덧붙입니다.
- 동작을 변경하면 관련 테스트, 설정 예시와 문서도 필요한 범위에서 함께 수정합니다.

## 실험과 재현성

- baseline 재현 결과와 VLADE 결과를 서로 다른 경로와 manifest로 관리합니다.
- 실행 기록에는 최소한 다음 정보를 남깁니다: 저장소 commit, submodule commit, 설정, checkpoint 식별자, dataset revision, seed, 하드웨어, 소프트웨어 환경과 평가 조건.
- 지연시간을 보고할 때 warm-up, 반복 횟수, batch size, dtype, 동기화 방식과 대상 하드웨어를 기록합니다.
- 논문 보고값, 외부 프로젝트 결과와 자체 측정값을 혼합하거나 덮어쓰지 않습니다.
- 실패한 실행도 원인 분석에 필요하면 상태와 오류를 manifest 또는 작업 기록에 남깁니다.

## 데이터와 실행 안전

- 데이터셋, 모델 가중치, 대규모 생성물과 비밀정보를 Git에 커밋하지 않습니다.
- 대용량 다운로드, 장시간 학습, GPU를 장시간 점유하는 작업 또는 비용이 발생할 수 있는 외부 서비스 호출은 실행 전에 사용자 확인을 받습니다.
- 먼저 최소 입력의 smoke test로 경로와 인터페이스를 확인한 뒤 큰 실험으로 확장합니다.
- 생성 파일은 목적에 따라 `.harness/`, `artifacts/`, `data/`, `checkpoints/` 중 올바른 위치에 둡니다.

## 검증

- 변경 전후로 Git 상태를 확인하여 작업 범위 밖의 변경을 건드리지 않았는지 점검합니다.
- 문서와 설정만 변경한 경우 링크, 경로, JSON/YAML 문법과 설명의 일관성을 확인합니다.
- Python 코드를 변경한 경우 관련 테스트와 최소 import/compile 검사를 수행합니다.
- 학습·평가 코드를 변경한 경우 가능한 가장 작은 smoke test를 우선 수행합니다.
- 실행하지 못한 검증은 이유와 함께 최종 보고에 명시하며 성공으로 간주하지 않습니다.

## Git과 완료 보고

- 파괴적인 Git 명령을 사용하지 않으며 사용자 작업을 임의로 삭제하지 않습니다.
- 요청 없이 commit, push, branch 변경 또는 submodule 갱신을 수행하지 않습니다.
- 완료 보고에는 변경한 내용, 검증한 항목과 남은 위험 또는 미검증 항목을 간단히 포함합니다.
